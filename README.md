# manim-growth

## Useful manim commands

`uv run manim checkhealth` performes a check and creates a test video.

Run your file with:
```
uv run manim -pql growth.py
```
The flag `p` plays the video, `q` and `l` set the quality to low.
